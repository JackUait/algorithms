// This function doesn't make sense actually

const l1=[2,4,3];
const l2=[5,6,4];

function solution(l1, l2) {
  return (((+l1.join("") + +l2.join("")) + '').split("").reverse()).map(parseFloat);
}

console.log(solution(l1,l2))
